export enum ServiceType
{
    Instance = 'instance',
    Singleton = 'singleton',
    Named = 'named',
    Scoped = 'scoped',
    ScopedNamed = 'scopednamed',
    Transient = 'transient',
    TransientNamed = 'transientnamed'
}
